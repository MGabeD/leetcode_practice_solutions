# backfill for 4/24

# Write your MySQL query statement below
select product_id, "store1" as store, store1 as price from Products
where store1 is not null
union all
select product_id, "store2" as store, store2 as price from Products
where store2 is not null
union all
select product_id, "store3" as store, store3 as price from Products
where store3 is not null

# it feels wrong to use this as a questions since it was an easy and in sql.... I think i will just because
# A) when I was doing these daily I was doing more than 1
# B) I am catching up and will be oging back to that so I just ened to catch up first before being picky if a question
# is hard enough to count for a day...